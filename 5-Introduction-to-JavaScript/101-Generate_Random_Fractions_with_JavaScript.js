// In this lesson: Math.random() generates a random fraction as a decimal between 0 (inclusive) and 1 (exclusive).

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
