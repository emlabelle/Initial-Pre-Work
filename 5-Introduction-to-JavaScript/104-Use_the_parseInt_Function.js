// In this lesson: The parseInt() function converts a string into its corresponding number. If the string does not correspond to a number, NaN is returned.

function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
