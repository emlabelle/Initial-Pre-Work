// In this lesson: Like if/else statements, conditional operators can be chained together.

function checkSign(num) {
  return (num === 0) ? "zero" : (num > 0) ? "positive" : "negative";
}

checkSign(10);
