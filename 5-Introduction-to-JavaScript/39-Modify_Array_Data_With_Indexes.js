// In this lesson: Unlike strings, bracket notation can be used to modify the contents of an array (because arrays are mutable and strings are immutable).

// Example
var ourArray = [18,64,99];
ourArray[1] = 45; // ourArray now equals [18,45,99].

// Setup
var myArray = [18,64,99];

// Only change code below this line.

myArray[0] = 45;
