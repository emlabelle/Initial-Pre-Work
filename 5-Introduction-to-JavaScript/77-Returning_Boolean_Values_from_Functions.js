// In this lesson: Since comparison operators produce a boolean value, instead of writing out an if statement to return true if true or return false if false, you can simply return the comparison statement for an identical outcome.

function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);
