// In this lesson: The rest operator "..." allows functions to take a variable number of arguments from an associated array.

const sum = (function() {
  "use strict";
  return function sum(...args) {
    return args.reduce((a, b) => a + b, 0);
  };
})();
console.log(sum(1, 2, 3)); // 6
